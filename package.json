{
    "dependencies": {
        "firebase": "^9.6.0",
        "fs": "^0.0.1-security",
        "xss": "^1.0.10"
    },
    "name": "listapp",
    "version": "1.0.0",
    "main": "index.js",
    "license": "MIT",
    "devDependencies": {
        "webpack": "^5.65.0",
        "webpack-cli": "^4.9.1",
        "webpack-dev-server": "^4.6.0"
    },
    "scripts": {
        "build": "firebase use test && NODE_ENV=dev webpack --mode development && cp public/index.html functions/hosting/ && firebase deploy --only hosting",
        "production": "firebase use prd && NODE_ENV=prd  webpack --mode production && cp public/index.html functions/hosting/ && firebase deploy --only hosting",
        "start": "NODE_ENV=dev webpack serve"
    },
    "repository": "https://github.com/kanaru-ssk/itl-02.git"
}
